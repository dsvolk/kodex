[package]
name = "kodex-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single Kodex workspace dependency.
# Add new Kodex surface area to `kodex-core-api` instead of depending on
# additional `kodex-*` crates here.
kodex-core-api = { workspace = true }
tracing = { workspace = true }
