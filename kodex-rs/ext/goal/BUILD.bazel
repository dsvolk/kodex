load("//:defs.bzl", "kodex_rust_crate")

kodex_rust_crate(
    name = "goal",
    compile_data = glob([
        "templates/**",
    ]),
    crate_name = "kodex_goal_extension",
    integration_compile_data_extra = [
        "src/accounting.rs",
        "src/steering.rs",
    ] + glob(["templates/**"]),
)
