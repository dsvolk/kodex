load("//bazel/rules:schema_bundle.bzl", "schema_bundle")

schema_bundle(
    name = "public-schema-bundle",
    testonly = True,
    generator = "//kodex-rs/app-server-protocol:schema-generator",
    tags = ["manual"],
)
